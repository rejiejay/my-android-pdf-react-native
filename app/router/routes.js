import List from './../view/list';
import PDF from './../view/pdf';

const routes = [
    {
        key: 'list',
        screen: List,
        props: {},
    }, 
    {
        key: 'pdf',
        screen: PDF,
        props: {},
    }
]

export default routes;
